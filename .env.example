# Copy to .env and set your values. Required when running this API standalone.
# Go API reads these via os.Getenv("POSTGRES_DB").

POSTGRES_DB=cats_database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432
